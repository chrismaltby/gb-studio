import React from "react";

const Modal = (props) => <div className="Modal" {...props} />;

const ModalFade = (props) => <div className="ModalFade" {...props} />;

const ModalContent = (props) => <div className="ModalContent" {...props} />;

export default Modal;

export { ModalFade, ModalContent };
